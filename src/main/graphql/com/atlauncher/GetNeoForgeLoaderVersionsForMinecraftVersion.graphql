query GetNeoForgeLoaderVersionsForMinecraftVersion($minecraftVersion: String!) {
    loaderVersions(minecraftVersion: $minecraftVersion) {
        neoforge {
            version
            rawVersion
            recommended
        }
    }
}
