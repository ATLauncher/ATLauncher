query GetPaperLoaderVersion($build: Int!, $minecraft: String!) {
    paperVersion(build: $build, minecraft: $minecraft) {
        build
        promoted
        filename
        downloadUrl
        sha256
    }
}
