query GetPaperLoaderVersionsForMinecraftVersion($minecraftVersion: String!) {
    loaderVersions(minecraftVersion: $minecraftVersion) {
        paper {
            build
            promoted
        }
    }
}
