query GetQuiltLoaderVersionsForMinecraftVersion($minecraftVersion: String!) {
    loaderVersions(minecraftVersion: $minecraftVersion) {
        quilt {
            version
            stable
        }
    }
}
