query GetPaperMCLoaderVersionsForMinecraftVersion($minecraftVersion: String!) {
    loaderVersions(minecraftVersion: $minecraftVersion) {
        papermc {
            build
            promoted
        }
    }
}
